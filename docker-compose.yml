version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - ./server/data:/app/data
    environment:
      - JWT_SECRET=please_change_this_secret
  client:
    build: ./client
    ports:
      - "5173:80"
    depends_on:
      - server